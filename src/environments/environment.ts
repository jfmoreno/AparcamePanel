// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.
/*
export const environment = {
  production: false,
};

*/
export const environment = {
  production: true,
  //dominio: 'https://appayservidor.herokuapp.com',
  dominio: 'http://localhost:5000',
  timeout: 0
};

/*
SMA08
NOMBRAR POR SU NOMBRE
foto estructura proyecto
foto estructura de la bbdd
si queremos comentar algo magnifico lo podemos conectar tambien
Pruebas para ver si tenemos errores o no
validar
lista de frameworks y plugins con lo que hemos trabajado
*/